fastapi
uvicorn[standard]
pandas
pyarrow
fastparquet
python-dotenv
python-multipart
# Model inference (CPU-only build — no GPU needed for loading weights)
torch --index-url https://download.pytorch.org/whl/cpu
scikit-learn
joblib
# PostgreSQL driver (production) — falls back to SQLite locally
psycopg2-binary
# Streaming JSON parse for large .gz ingestion
ijson
# AI suggestion providers (lazy-imported — all three included so any key works)
google-genai        # Gemini 2.0/2.5 Flash  — GEMINI_API_KEY
openai>=1.0         # DeepSeek (OpenAI-compat) — DEEPSEEK_API_KEY
groq                # Groq / Llama-3.3-70b  — GROQ_API_KEY
