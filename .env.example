# Leave blank to use SQLite (local dev). Set to Neon/Supabase URL for production.
DATABASE_URL=
DATA_DIR=./data/processed
DEMO_DIR=./demo
DEMO_MODE=false
PORT=8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# AI investigation suggestions — set ONE of the following (first found wins):
# GEMINI_API_KEY   — https://aistudio.google.com/apikey  (completely free, no credit card)
# DEEPSEEK_API_KEY — https://platform.deepseek.com       (free credits on sign-up)
# GROQ_API_KEY     — https://console.groq.com            (free tier, fastest)
GEMINI_API_KEY=
