# Evaluation script dependencies (not needed for the backend server)
# Install with:  pip install -r scripts/requirements-eval.txt

matplotlib>=3.7
numpy>=1.24
scikit-learn>=1.3
pandas>=2.0
pyarrow>=12.0        # needed for reading .parquet files
